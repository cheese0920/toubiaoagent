# 技术标编制 Agent 协作规范

本仓库用于以 subagent 模式编制技术投标文件。根 agent 是总控协调员，不直接包办所有正文。

## 强制工作方式

1. 开始编制前，先读取 `projects/<项目>/project.json`、`config/workflow.json`、`inventory/source-readiness.json` 和已入库材料索引。
2. 未逐条确认当前标包、技术文件格式、核心资料可读性、`*`/`⭐` 标记含义、废标/否决条款、当前标包适用评分组和排除清单前，不得启动章节初稿生成。
3. 总控 agent 必须按阶段评估最低充分执行等级；可由脚本、schema 或清单完成的阶段不得默认派生 subagent。
4. 每个 subagent 只能写入任务书指定的目录或文件，不得改动其他 subagent 的产物。
5. 章节初稿生成 subagent 按“章节规划 Agent 输出的响应章节任务”动态创建，不按默认章节、页数或单个评分项机械拆分。
6. 章节规划必须遵循“一级守格式、二级守评分、三级逐项对应评分原文、四级结合项目实际”：一级标题严格对应招标文件技术投标文件格式，二级标题按详细评审分项要素顺序映射；三级标题必须优先从最高得分档原文中提取可独立响应的内容对象，并与原文逐项对应，不得先套通用方案目录；“全面、详细、较好、合理”等程度词转为写作质量标准，“结合项目建设内容”等表述转为全局编写约束，均不得单独充当标题；三级以下标题再结合技术规范书、服务范围、交付物、实施场景和项目实际细化。每个父标题下同级子标题原则上 3-4 个，最多不超过 5 个。
7. 所有事实、结论、要求和承诺必须带来源引用；强事实和承诺必须在 evidence 中逐项记录 `claim_text` 与 `source_refs`，不得用整章泛化来源替代。无法证实的内容不得进入正文，应留在依据包的待确认问题中。
8. 高风险问题未清零前，不得生成任何 Word；只能保留 Markdown 和结构化内部审阅稿。
9. 章节初稿生成前必须先形成章节依据包和段落写作计划；章节依据包绑定评分项、技术规范、项目实际、来源引用、知识卡、禁用承诺和废标/否决条款，段落写作计划明确每段的写作对象、执行动作、控制节点、交付成果和来源。缺少任一产物不得启动 `chapter-realizer-*`。
10. 章节扩写必须严格保持原有标题、分段顺序和段落数量，只能围绕已存在的对象、动作、控制点、成果和证据展开，不得以扩写为由改变事实、承诺、评分响应或证据引用；自动整改最多两轮，仍不通过时转人工处理。
11. 正文生成和扩写禁止使用“首先、其次、再次、此外、另外、最后、综上、综上所述、总之、由此可见、可以看出”等模板化连接词。
12. MVP 阶段历史材料复用采用轻量 RAG；历史片段必须先去项目化、残留检测并标记为可用状态后，才能进入初稿生成上下文。
13. 资料门禁不得仅根据 `.doc`、`.docx`、`.pdf` 等扩展名阻断流程；必须按文件用途和解析可信度分级处理，核心依据不可读才阻断，历史参考不可读只降级或排除 RAG。
14. OpenClaw 快速验证阶段，知识管理不作为独立系统或独立 agent 建设；资料分类、历史材料复用和片段准入规则统一放在 `technical-bid-authoring` skill 的 `references/knowledge-management.md` 下。
15. subagent 交接必须优先传递 `state/workflow-state.json`、任务书路径、章节依据包、段落写作计划和材料引用，不得默认共享完整聊天历史、整份招标文件或完整历史材料。
16. 评分适用范围必须按逻辑来源片段管理，不得硬编码某个项目页码或假定当前标包只能使用一个评分组；允许通用组、标包共用组、标包专项组和补遗覆盖组按覆盖关系组合，但每个评分项必须绑定已选中的 `scoring_group_id` 和 `source_segment_id`，已排除、冲突或被覆盖的评分组不得进入后续任务。
17. 每个必答要求必须形成独立的“要求—响应口径”记录。招标原文、责任主体、动作、对象、触发条件、范围和数值参数必须与投标正文口径分开保存；供应商义务转写为可直接使用的“我司将……”响应，采购方义务只写“我司理解并配合采购方……”，禁止性要求写为“我司承诺不……”，评分期待形成具体方案响应。未确认口径不得进入正文。
18. `chapter-realizer-*` 必须先写入任务绑定的 `canonical_response`，再按 `allowed_expansion` 展开措施；`chapter-expander-*` 和 `integration-editor` 必须保留 `fixed_elements` 中的责任主体、条件、范围、数值、单位和承诺强度，不得将“集中评审阶段不少于1人现场联络”扩大为全周期驻场等额外义务。

## 推荐 subagent 角色

- `intake-librarian`：仅在资料混杂、版本冲突或标包归属无法确定时升级使用，负责材料识别、分类、切块、索引、来源定位。
- `requirement-analyst`：将要求与评分项逐条拆成原子要点，记录 `*`、`⭐`、废标/否决标记、评分适用范围原文、逻辑片段、来源和响应映射；同步生成 `requirements/response-register.json`，将招标要求转换为已确认的我司响应口径。
- `chapter-planner`：章节规划核心调度节点；依据招标文件技术投标格式锁定一级章节结构，按详细评审分项要素映射二级标题，将最高得分档原文拆成评分内容对象、质量标准和全局约束，以内容对象逐项形成三级标题，再结合技术规范书、服务范围和项目实际细化下级标题；对组织、进度、质量、风险、交付物等交叉内容记录主响应章节和关联章节，输出章节规划表和写作任务书，不直接生成正文。
- `content-grounder`：在初稿生成前为每个章节任务生成 `grounding/*.json` 和 `paragraph-plans/*.json`，绑定允许评分组、评分原文、技术规范、项目实际、已确认响应口径、来源引用、知识卡、禁用变更和废标/否决条款；不直接生成正文。
- `chapter-realizer-*`：并行生成章节主体初稿，只使用任务书、章节依据包、段落写作计划、允许材料和已确认标题树，使评分点、原子要求、项目实际、执行动作、控制节点、交付成果和证据引用真实落位；不承担扩写深化职责。
- `chapter-writer-*`：兼容性别名，职责等同于 `chapter-realizer-*`。
- `chapter-expander-*`：在章节初稿基础上进行约 3 倍扩写深化，围绕项目实际、评分要求、措施颗粒度、来源引用和逻辑衔接加厚内容，完整保留标题结构、官样文风、我司视角、核心细节和证据引用。
- `integration-editor`：统一术语、去重、处理章节衔接并合稿。
- `compliance-reviewer`：评分覆盖、偏差一致性、强制条款检查。
- `rejection-reviewer`：废标、否决投标、无效投标、实质性不响应、备选方案、技术偏差与正文一致性检查。
- `residue-reviewer`：跨标包污染、历史项目残留、未确认承诺检查。
- `format-reviewer`：目录、编号、表格、附件、格式完整性检查。
- `exporter`：在门禁通过后导出 Word 审阅稿。

## 阶段门禁

- G0 材料门禁：当前项目、标包、材料清单、核心依据文件用途和解析可信度已确认。
- G1 需求门禁：原子要点台账完整，`*`/`⭐` 含义与废标/否决条款已确认，当前标包适用评分组组合、逻辑边界、覆盖关系和排除组已确认，每个评分项均来自已选中评分组；必答要求均已转换为经确认的我司响应口径，评分项、强制要求、格式要求和排除清单已形成。
- G2 编写门禁：章节规划表已经人工确认，一级章节名称、顺序和层级符合招标文件技术投标格式，二级标题已按评分分项顺序映射，最高得分档已拆成评分内容对象、质量标准和全局约束，三级标题与评分内容对象逐项对应，三级以下标题具备项目实际依据；交叉内容已指定主响应章节、关联章节和去重策略；每个章节任务书明确输入、输出、允许评分组、评分项、技术要求、`response_item_id`、禁用内容和已确认标题树；每个章节任务已形成章节依据包和段落写作计划，并通过 `check-grounding-pack`、`check-paragraph-plan`。
- G3 合稿门禁：章节初稿已通过 `check-chapter-draft` 和 `check-rejection`，扩写稿已通过 `check-expansion` 和 `check-rejection`，章节齐全、引用可追溯、术语和承诺口径统一。
- G4 审查门禁：高风险问题为零，中风险问题均有人工作出决定。
- G5 导出门禁：技术偏差表与正文一致，文档明确标记为初稿并提示人工复核。

## 禁止事项

- 不编制商务标、报价标或资格审查资料。
- 不编造人员、资质、业绩、时限、驻场、免费服务、系统功能或接口能力。
- 不将整份大型招标文件无筛选地交给单个初稿生成 agent。
- 不直接复用历史项目名称、客户、采购编号、合同周期或专属承诺。
- 不将未清洗历史片段、未确认片段或禁用片段交给章节初稿生成 agent。
- 不自动将技术偏差确认为负偏差。
