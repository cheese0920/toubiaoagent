# 技术标编制 Agent 协作规范

本仓库用于以 subagent 模式编制技术投标文件。根 agent 是总控协调员，不直接包办所有正文。

## 强制工作方式

1. 开始编制前，先读取 `projects/<项目>/project.json`、`config/workflow.json`、`inventory/source-readiness.json` 和已入库材料索引。
2. 未逐条确认当前标包、技术文件格式、核心资料可读性、`*`/`⭐` 标记含义、废标/否决条款、当前标包适用评分组和排除清单前，不得启动正文编写。
3. 总控 agent 必须按阶段评估最低充分执行等级；可由脚本、schema 或清单完成的阶段不得默认派生 subagent。
4. 每个 subagent 只能写入任务书指定的目录或文件，不得改动其他 subagent 的产物。
5. 正文编写 subagent 按“章节规划 Agent 输出的响应章节任务”动态创建，不按默认章节或单个评分项机械拆分。
6. 所有事实、结论、要求和承诺必须带来源引用或标记为待人工确认。
7. 高风险问题未清零前，不得生成任何 Word；只能保留 Markdown 和结构化内部审阅稿。
8. 章节扩写必须严格保持原有标题、分段顺序和段落数量，不得以扩写为由改变事实、承诺、评分响应或证据引用。
9. MVP 阶段历史材料复用采用轻量 RAG；历史片段必须先去项目化、残留检测并标记为可用状态后，才能进入 writer 上下文。
10. 资料门禁不得仅根据 `.doc`、`.docx`、`.pdf` 等扩展名阻断流程；必须按文件用途和解析可信度分级处理，核心依据不可读才阻断，历史参考不可读只降级或排除 RAG。
11. OpenClaw 快速验证阶段，知识管理不作为独立系统或独立 agent 建设；资料分类、历史材料复用和片段准入规则统一放在 `technical-bid-authoring` skill 的 `references/knowledge-management.md` 下。
12. subagent 交接必须优先传递 `state/workflow-state.json`、任务书路径和材料引用，不得默认共享完整聊天历史、整份招标文件或完整历史材料。

## 推荐 subagent 角色

- `intake-librarian`：仅在资料混杂、版本冲突或标包归属无法确定时升级使用，负责材料识别、分类、切块、索引、来源定位。
- `requirement-analyst`：将要求与评分项逐条拆成原子要点，记录 `*`、`⭐`、废标/否决标记、评分适用范围原文、来源和响应映射。
- `chapter-planner`：章节规划核心调度节点；依据招标文件技术投标格式锁定一级章节结构，并结合技术评分项、技术规范书、服务范围和评分内容体量进行二级/三级拆分，输出章节规划表和写作任务书，不直接生成正文。
- `chapter-writer-*`：并行编写各响应章节，只使用任务书允许的材料。
- `chapter-expander-*`：在原章节结构内进行约 3 倍扩写，完整保留官样文风、我司视角、核心细节和证据引用。
- `integration-editor`：统一术语、去重、处理章节衔接并合稿。
- `compliance-reviewer`：评分覆盖、偏差一致性、强制条款检查。
- `residue-reviewer`：跨标包污染、历史项目残留、未确认承诺检查。
- `format-reviewer`：目录、编号、表格、附件、格式完整性检查。
- `exporter`：在门禁通过后导出 Word 审阅稿。

## 阶段门禁

- G0 材料门禁：当前项目、标包、材料清单、核心依据文件用途和解析可信度已确认。
- G1 需求门禁：原子要点台账完整，`*`/`⭐` 含义与废标/否决条款已确认，当前标包适用评分组已唯一确认，评分项、强制要求、格式要求和排除清单已形成。
- G2 编写门禁：章节规划表已经人工确认，一级章节名称、顺序和层级符合招标文件技术投标格式；每个章节任务书明确输入、输出、评分项、技术要求和禁用内容。
- G3 合稿门禁：章节齐全、引用可追溯、术语和承诺口径统一。
- G4 审查门禁：高风险问题为零，中风险问题均有人工作出决定。
- G5 导出门禁：技术偏差表与正文一致，文档明确标记为初稿并提示人工复核。

## 禁止事项

- 不编制商务标、报价标或资格审查资料。
- 不编造人员、资质、业绩、时限、驻场、免费服务、系统功能或接口能力。
- 不将整份大型招标文件无筛选地交给单个编写 agent。
- 不直接复用历史项目名称、客户、采购编号、合同周期或专属承诺。
- 不将未清洗历史片段、未确认片段或禁用片段交给正文编写 agent。
- 不自动将技术偏差确认为负偏差。
